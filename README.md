# Desktop Lyrics Frontend

A static desktop-style lyrics frontend for a music app. It includes synced lyric highlighting, playback controls, a queue panel, responsive layout, and display settings for lyric size, spacing, focus dimming, and accent color.

## Run

Open `index.html` in a browser.

## Test

```bash
node --test tests/frontend-contract.test.js
```
